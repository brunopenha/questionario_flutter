name: questionario
description: Questionario para programadores.
version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  mockito:
  faker:
  

flutter:
  uses-material-design: true
  